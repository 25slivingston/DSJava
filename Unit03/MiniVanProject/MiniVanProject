public class ATLesson05 {
    public static void main(String[] args) {

        class DoorSystem {
    enum GearState {PARK, NEUTRAL, DRIVE, REVERSE, GEAR1, GEAR2, GEAR3}
    Gearstate CurrentGear = Gearstate.PARK;

    boolean DashLeft = false;
    boolean DashRight = false; //false means no one is pressing the button

    boolean InsideLeft = false;
    Boolean InsideRight = false;

    boolean OutsideLeft = false;
    boolean OutsideRight = false;

    //master unlock swich if true doors are unlocked
    boolean MasterUnlock = true;
    //if true inside handles don't work
    boolean ChildLock = false;

    //create a method to update the state of the system
    public void updateState(String newState) {
        //string has to bea at least 9 characters long
        if (newState.length() >= 9) {
            //extract state from the string
            if (newState.charAt[0] == '1') {

                dashLeft = true;
                else{
                    dashLedft = false;

            if (newState.charAt[2] == '1') {

                 childLock = true;
                else{
                    childLock = false;
                    

            if (newState.charAt[0] == '1') {

                dashLeft = true;
                else{
                    dashLedft = false;




                    if (newState.charAt[8] == 'p') {

                currentGear = GearState.PARK;

                else if { (newState.charAt[8] == 'd')
                    dashLedft = false;
                    currentGear = GearState.DRIVE;



            public boolean isLeftOpening() {

                return false;

                    }



                    

                }
            }
        }

    }


    

    


    }
}
}